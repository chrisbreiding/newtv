<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>TV Episodes</title>
	<link rel="shortcut icon" href="/favicon.ico" type="image/x-icon" />
  <meta name='robots' content='noindex,nofollow' />
	<%= stylesheet_link_tag "application", :media => "all" %>
	<%= csrf_meta_tags %>
</head>
<body>
	<%= yield %>

	<% if notice %>
		<div class="notice message clearfix">
			<p><%= notice %></p>
			<p><a href="#" class="close-notice">Close</a></p>
		</div>
	<% end %>

	<% if alert %>
		<div class="notice error clearfix">
			<p><%= alert %></p>
			<p><a href="#" class="close-notice">Close</a></p>
		</div>
	<% end %>

	<%= javascript_include_tag "application" %>
</body>
</html>
