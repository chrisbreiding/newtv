<%= link_to 'Add Show', new_show_path, id: 'add-show', class: 'add-show' %>

<%= render 'shows', shows: @shows %>

<%= render 'off_air', off_air: @off_air %>

<%= render 'jst_add_show' %>

<%= render 'jst_search_results' %>